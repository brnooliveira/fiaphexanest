name: Criar e publicar imagem docker
on:
  push:
    tags: [ "v[0-9]+.[0-9]+.[0-9]+" ]
jobs:
  build_image:
    runs-on: ubuntu-latest

    steps:
      - name: Configurar o Buildx
        uses: docker/setup-buildx-action@v3

      - name: Fazer login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: cviniciussdias
          password: "dckr_pat_F-7t5AZ5yJfIwnh8M1dLBRW2BEI"

      - name: Extrair versão da tag
        id: extract_version
        run: echo "::set-output name=version::${GITHUB_REF/refs\/tags\//}"

      - name: Build e push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: cviniciussdias/grupo61:latest, cviniciussdias/grupo61:${{ steps.extract_version.outputs.version }}
          file: "docker/app.dockerfile"

